import 'package:flutter/material.dart';

class KeyboardRowElement extends StatelessWidget {
  const KeyboardRowElement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Icon(Icons.add), width: 50,);
  }
}
