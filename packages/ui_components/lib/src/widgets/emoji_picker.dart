import 'package:flutter/material.dart';

class UIEmojiPicker extends StatelessWidget {
  const UIEmojiPicker({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 450,
      
    );
  }
}
