import 'package:flutter/material.dart';
import 'package:material_symbols_icons/symbols.dart';

/// all static icons
class UIIcons {
  /// arrow forward to indicate that an element is clickable
  static const arrowForward = Icon(Symbols.arrow_forward_ios, size: 32);
  static const add = Icon(
    Symbols.add_rounded,
    size: 32,
  );
  static const download = Icon(Symbols.download, size: 32);
}
